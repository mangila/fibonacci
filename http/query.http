### POST id request to SSE endpoint
POST http://{{baseUrl}}/api/v1/sse/option
Content-Type: application/json

{
  "sseSubscription": {
    "channel": "{{channel}}",
    "username": "{{username}}"
  },
  "option" : {
    "offset" : 1,
    "limit" : 10
  }
}